{
  "name": "autoescola",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "node scripts/prepare-data.mjs && vite --host 0.0.0.0",
    "build": "node scripts/prepare-data.mjs && tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "typescript": "^5.8.0",
    "tsx": "^4.20.0",
    "fake-indexeddb": "^6.0.0",
    "@types/node": "^22.0.0"
  }
}
